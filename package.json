{
  "name": "who-pays-donors",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt --hostname 'localhost' --port 3000",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "d3": "^6.2.0",
    "nuxt": "^2.14.6"
  },
  "devDependencies": {
    "@nuxt/content": "^1.10.0",
    "@nuxtjs/style-resources": "^1.0.0",
    "@nuxtjs/vuetify": "^1.11.2",
    "fibers": "^5.0.0",
    "push-dir": "^0.4.1",
    "sass": "^1.28.0",
    "sass-loader": "^10.0.4"
  }
}
